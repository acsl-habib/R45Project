# R45Project
In SmartHR.DataApi Project Folder
run
1. dotnet ef database update -c ApplicationDbContext
2. dotnet ef database update -c HRDbContext 

In SmartHR.DataApi Project Folder
run 
dotnet run

In ClientApp folder under SmartHR.Client
Run
ng serve -o
