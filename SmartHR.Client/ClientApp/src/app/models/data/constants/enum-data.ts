export enum OfficeHourType {
  Regular = 1, Ramadan
}
export enum Gender { Male = 1, Female }
export enum MaritalStatus { Married = 1, Unmarried }
export enum EmployeeStatus { Active = 1, OnLeave, Suspended, Inactive, Terminated, Left, Released  }
export enum CalculationType { Flat = 1, Percentage }
